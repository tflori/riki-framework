version: "2"

checks:
    method-complexity:
        config:
            threshold: 9

exclude_patterns:
    - "docs/"
    - "**/tests/"
    - "**/vendor/"

plugins:
    phpcodesniffer:
        enabled: true
        config:
            standard: "PSR2"
